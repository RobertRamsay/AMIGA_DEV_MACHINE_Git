diff --git a/AMIGA_DEV_MACHINE.yyp b/AMIGA_DEV_MACHINE.yyp
index dcb8a4e..db76822 100644
--- a/AMIGA_DEV_MACHINE.yyp
+++ b/AMIGA_DEV_MACHINE.yyp
@@ -59,6 +59,7 @@
     {"id":{"name":"scr_pick_default_size","path":"scripts/scr_pick_default_size/scr_pick_default_size.yy",},},
     {"id":{"name":"scr_save_layout","path":"scripts/scr_save_layout/scr_save_layout.yy",},},
     {"id":{"name":"scr_snap_to_grid","path":"scripts/scr_snap_to_grid/scr_snap_to_grid.yy",},},
+    {"id":{"name":"scr_check_node_overlap","path":"scripts/scr_check_node_overlap/scr_check_node_overlap.yy",},},
     {"id":{"name":"scr_split_lines","path":"scripts/scr_split_lines/scr_split_lines.yy",},},
     {"id":{"name":"scr_try_stack_connect","path":"scripts/scr_try_stack_connect/scr_try_stack_connect.yy",},},
     {"id":{"name":"scr_validate_node_addressing","path":"scripts/scr_validate_node_addressing/scr_validate_node_addressing.yy",},},
diff --git a/objects/obj_opcode_node/Create_0.gml b/objects/obj_opcode_node/Create_0.gml
index fed233c..26ef11f 100644
--- a/objects/obj_opcode_node/Create_0.gml
+++ b/objects/obj_opcode_node/Create_0.gml
@@ -10,6 +10,8 @@ is_dragging = false;
 is_selected = false;
 drag_offset_x = 0;
 drag_offset_y = 0;
+grab_start_x = 0;
+grab_start_y = 0;
 
 is_connected = false;
 parent_uid = -1;
diff --git a/objects/obj_opcode_node/Step_0.gml b/objects/obj_opcode_node/Step_0.gml
index 0dec655..966c632 100644
--- a/objects/obj_opcode_node/Step_0.gml
+++ b/objects/obj_opcode_node/Step_0.gml
@@ -14,6 +14,14 @@ if (is_dragging) {
 
     if (mouse_check_button_released(mb_left)) {
         is_dragging = false;
+
+        var _would_overlap = scr_check_node_overlap(id, node_x, node_y);
+
+        if (_would_overlap) {
+            node_x = grab_start_x;
+            node_y = grab_start_y;
+        }
+
         scr_try_stack_connect(id);
     }
 } else {
@@ -23,10 +31,20 @@ if (is_dragging) {
         is_dragging = true;
         drag_offset_x = node_x - mouse_x;
         drag_offset_y = node_y - mouse_y;
+        grab_start_x = node_x;
+        grab_start_y = node_y;
         is_selected = true;
         is_connected = false;
         parent_uid = -1;
     }
+
+    if (_over_body && mouse_check_button_pressed(mb_right)) {
+        if (global.operand_edit_owner_uid == uid) {
+            global.operand_edit_owner_uid = -1;
+        }
+
+        instance_destroy();
+    }
 }
 
 mode_button_width = node_width;
diff --git a/scripts/scr_check_node_overlap/scr_check_node_overlap.gml b/scripts/scr_check_node_overlap/scr_check_node_overlap.gml
new file mode 100644
index 0000000..d75cedb
--- /dev/null
+++ b/scripts/scr_check_node_overlap/scr_check_node_overlap.gml
@@ -0,0 +1,33 @@
+/// @desc scr_check_node_overlap(_self_id, _test_x, _test_y)
+/// Returns true if a node of _self_id's size, placed at (_test_x, _test_y),
+/// would overlap any other obj_opcode_node or obj_amiga_root_node.
+/// Exact edge-touching (normal stacking) is NOT counted as overlap.
+function scr_check_node_overlap(_self_id, _test_x, _test_y) {
+    var _has_overlap = false;
+
+    with (obj_opcode_node) {
+        var _is_self = (id == _self_id);
+
+        if (!_is_self) {
+            var _overlap_x = (_test_x < node_x + node_width) && (_test_x + _self_id.node_width > node_x);
+            var _overlap_y = (_test_y < node_y + node_height) && (_test_y + _self_id.node_height > node_y);
+
+            if (_overlap_x && _overlap_y) {
+                _has_overlap = true;
+            }
+        }
+    }
+
+    if (!_has_overlap) {
+        with (obj_amiga_root_node) {
+            var _overlap_x = (_test_x < node_x + node_width) && (_test_x + _self_id.node_width > node_x);
+            var _overlap_y = (_test_y < node_y + node_height) && (_test_y + _self_id.node_height > node_y);
+
+            if (_overlap_x && _overlap_y) {
+                _has_overlap = true;
+            }
+        }
+    }
+
+    return _has_overlap;
+}
diff --git a/scripts/scr_check_node_overlap/scr_check_node_overlap.yy b/scripts/scr_check_node_overlap/scr_check_node_overlap.yy
new file mode 100644
index 0000000..7738f84
--- /dev/null
+++ b/scripts/scr_check_node_overlap/scr_check_node_overlap.yy
@@ -0,0 +1,13 @@
+{
+  "$GMScript":"v1",
+  "%Name":"scr_check_node_overlap",
+  "isCompatibility":false,
+  "isDnD":false,
+  "name":"scr_check_node_overlap",
+  "parent":{
+    "name":"AMIGA_DEV_MACHINE",
+    "path":"AMIGA_DEV_MACHINE.yyp",
+  },
+  "resourceType":"GMScript",
+  "resourceVersion":"2.0",
+}
